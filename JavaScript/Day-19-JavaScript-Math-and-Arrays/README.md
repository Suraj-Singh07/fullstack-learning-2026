# Day 19 - JavaScript Math and Arrays

This folder contains my Day 19 JavaScript practice from my **FullStack Learning 2026** journey.

## Topics Learned

* `while` loop
* Nested loops
* `Math` object methods
* Random number generation
* Number guessing game
* Array basics
* Array length
* Mixed data type arrays

## Concepts Practiced

### While Loop

* Countdown using `while`
* Multiplication table using `while`
* Printing each character of a string

### Nested Loops

* Printing patterns with nested loops
* Printing letters **A–Z** with numbers **1–10**

### Math Object

* `Math.sqrt()`
* `Math.PI`
* `Math.pow()`
* `Math.random()`
* `Math.round()`

### Programs Built

1. Random number generator.
2. Number guessing game (1–10).
3. Number guessing game with hints (1–100).

### Arrays

* Creating arrays
* Accessing array elements
* Using `.length`
* Arrays with different data types

Example:

```javascript
[1, 2, "A", true]
```

This is still an array because JavaScript arrays can store values of different data types.

## Technologies Used

* HTML5
* JavaScript (ES6)
